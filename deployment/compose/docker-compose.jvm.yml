services:
  api:
    build:
      context: ../..
      dockerfile: deployment/docker/Dockerfile
    mem_limit: 512m
    environment:
      JAVA_TOOL_OPTIONS: "${JAVA_TOOL_OPTIONS:--XX:MaxRAMPercentage=50 -XX:InitialRAMPercentage=15 -XX:MaxMetaspaceSize=192m -XX:+UseSerialGC}"

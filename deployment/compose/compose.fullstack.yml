services:
  web:
    build:
      context: ../../../insurance-quotes-web
      args:
        VITE_API_BASE_URL: /api
    ports:
      - "3100:80"
    depends_on:
      api:
        condition: service_healthy

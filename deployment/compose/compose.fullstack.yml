services:
  web:
    build:
      context: ../../../insurance-quotes-web
      args:
        VITE_API_BASE_URL: http://localhost:8080
    ports:
      - "3000:80"
    depends_on:
      api:
        condition: service_healthy

/**
 * Authentication module. It owns authentication, session, and passkey workflows; other modules use only its explicitly named API packages.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Authentication")
package com.clara.insurancequotes.auth;

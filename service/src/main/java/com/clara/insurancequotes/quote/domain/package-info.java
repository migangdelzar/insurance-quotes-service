/**
 * Quote lifecycle business logic, state, and invariants.
 *
 * <p>It must not depend on web controllers, HTTP DTOs, JPA entities or
 * repositories, caches, messaging, external clients, or Spring framework
 * concerns.</p>
 */
package com.clara.insurancequotes.quote.domain;

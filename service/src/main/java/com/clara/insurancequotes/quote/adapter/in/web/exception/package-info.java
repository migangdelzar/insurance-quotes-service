/**
 * Transport-specific failures raised while parsing or validating Quote HTTP input.
 * These exceptions stay inside the web adapter and are translated to HTTP errors
 * by the adjacent advice package.
 */
package com.clara.insurancequotes.quote.adapter.in.web.exception;

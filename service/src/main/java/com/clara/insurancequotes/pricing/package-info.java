/**
 * Pricing module. It owns premium calculation; other modules use only its explicitly named API packages.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Pricing")
package com.clara.insurancequotes.pricing;

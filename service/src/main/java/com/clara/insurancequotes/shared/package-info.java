/**
 * Shared cross-cutting contracts and technical support. It is not a business
 * domain and exposes only deliberately named shared contracts; it must not
 * absorb module-specific business rules.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Shared")
package com.clara.insurancequotes.shared;

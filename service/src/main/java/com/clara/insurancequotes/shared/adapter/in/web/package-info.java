/**
 * Shared web adapter infrastructure. It handles HTTP-specific concerns and
 * must not contain module business rules or persistence implementation.
 */
package com.clara.insurancequotes.shared.adapter.in.web;
